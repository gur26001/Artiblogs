// const User = require('../model/user.modal')

// // create a new user on reaching signing-sign in

// async function newUser(req,res){

// }
// // login - checking a user exists or not, if it is then show it
// async function checkUser(req, res) {
//     //check if user exists
//     // if exists then check thier password by hashing
//     // if not then give user doesn't exists
// }
